{
  "name": "braidstudio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check public/app.js && node --check scripts/textile-proof-screenshot.mjs && python3 -c \"import ast,pathlib; ast.parse(pathlib.Path('scripts/braid_geometry.py').read_text())\"",
    "build": "npm run check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright-core": "^1.61.1"
  }
}
